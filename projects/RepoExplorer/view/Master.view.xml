<core:View
	controllerName="oscon2014.repoexplorer.view.Master"
	xmlns="sap.m"
	xmlns:core="sap.ui.core" >
	<Page
		title="{i18n>MasterTitle}" >
		<subHeader>
			<Bar>
				<contentLeft>
					<SearchField
						search="handleSearch"
						width="100%" >
					</SearchField>
				</contentLeft>
			</Bar>
		</subHeader>
		<List
			id="list"
			mode="{device>/listMode}"
			select="handleListSelect"
			items="{/repos}" >
			<ObjectListItem
				icon="sap-icon://detail-view"
				type="{device>/listItemType}"
				press="handleListItemPress"
				title="{name}"
				number="{stargazers_count}"
				numberUnit="Starred">
				<attributes>
					<ObjectAttribute
						text="Updated {
							path: 'updated_at',
							formatter: 'oscon2014.repoexplorer.util.Formatter.fromNow'
						}" />
				</attributes>
				<firstStatus>
					<ObjectStatus
						text="{language}" />
				</firstStatus>
			</ObjectListItem>
		</List>
		<footer>
			<Bar>
				<contentRight>
					<Select
						id="groupSelect"
						change="handleGroup"
						icon="sap-icon://group-2"
						type="IconOnly"
						selectedKey="None"
						autoAdjustWidth="true" >
						<core:Item
							key="None"
							text="{i18n>MasterGroupNone}"/>
						<core:Item
							key="GrossAmount"
							text="{i18n>MasterGroupAmount}"/>
						<core:Item
							key="LifecycleStatus"
							text="{i18n>MasterGroupStatus}"/>
					</Select>
				</contentRight>
			</Bar>
		</footer>
	</Page>
</core:View>
